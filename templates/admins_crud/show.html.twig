{% extends 'admin_services/backendAdmin.html.twig' %}
{% block central %}


   <div class="container">

       <table id="articles" class="table  table-bordered table-hover table-dark   ">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ user.nom }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ user.role }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ user.prenom }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ user.photo }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>


        </tbody>
    </table>

    <a href="{{ path('admins_crud_index') }}" class="btn btn-info">Back</a>

    <a href="{{ path('admins_crud_edit', {'id': user.id}) }}" class="btn btn-secondary">edit</a>


   </div>
{% endblock %}
